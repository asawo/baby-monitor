version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign
    - unused
    - misspell

formatters:
  enable:
    - goimports
  settings:
    goimports:
      local-prefixes:
        - babymonitor
